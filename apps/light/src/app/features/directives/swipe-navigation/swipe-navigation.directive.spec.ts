import { SwipeNavigationDirective } from './swipe-navigation.directive';

describe('SwipeNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
