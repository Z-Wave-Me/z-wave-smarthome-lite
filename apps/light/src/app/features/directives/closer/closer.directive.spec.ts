import { CloserDirective } from './closer.directive';

describe('CloserDirective', () => {
  it('should create an instance', () => {
    const directive = new CloserDirective();
    expect(directive).toBeTruthy();
  });
});
