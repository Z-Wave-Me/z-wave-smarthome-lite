import { RestorePositionDirective } from './restore-position.directive';

describe('RestorePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new RestorePositionDirective();
    expect(directive).toBeTruthy();
  });
});
