import { SignificantPipe } from './significant.pipe';

describe('SignificantPipe', () => {
  it('create an instance', () => {
    const pipe = new SignificantPipe();
    expect(pipe).toBeTruthy();
  });
});
